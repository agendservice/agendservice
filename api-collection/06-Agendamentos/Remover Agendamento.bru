meta {
  name: Remover Agendamento
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/api/agendamentos/{{agendamentoId}}
  body: none
  auth: none
}

tests {
  test("Status deve ser 204", function() {
    expect([200, 204]).to.include(res.status);
  });
}
