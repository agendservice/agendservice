meta {
  name: Ver Servico
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/api/servicos/{{servicoId}}
  body: none
  auth: none
}

tests {
  test("Status deve ser 200", function() {
    expect(res.status).to.equal(200);
  });
}
