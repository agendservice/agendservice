meta {
  name: Historico do Cliente
  type: http
  seq: 5
}

get {
  url: {{baseUrl}}/api/clientes/{{clienteId}}/historico
  body: none
  auth: none
}

tests {
  test("Status deve ser 200", function() {
    expect(res.status).to.equal(200);
  });
}
